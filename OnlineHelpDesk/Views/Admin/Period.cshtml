@model IEnumerable<OnlineHelpDesk.Models.Account>

@{
    ViewData["Title"] = "List Period";
}
<div class="content-wrapper">
    <!-- Main content -->
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <h3>@ViewBag.msg</h3>
                    <p>
                        <a asp-controller="Period" asp-action="Create">Create New</a>
                    </p>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    Name
                                </th>
                                <th>
                                    Status
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in ViewBag.period)
                            {
                            <tr>
                                <td>
                                    @item.Name
                                </td>
                                <td>
                                    @(item.Status ? "Active" : "Disable")
                                </td>
                                <td>
                                    <a asp-controller="Period" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                                    <a asp-controller="Period" asp-action="Delete" asp-route-id="@item.Id" OnClick="return confirm('Are you sure?')">Delete</a>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
